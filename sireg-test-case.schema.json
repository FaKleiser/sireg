{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "sireg test case",
  "description": "A test case definition that is understood by https://github.com/FaKeller/sireg",
  "type": "object",
  "properties": {
    "testCase": {
      "type": "string"
    },
    "loaders": {
      "type": "array",
      "items": {
        "oneOf": [
          { "$ref": "#/definitions/FileLoader" },
          { "$ref": "#/definitions/SitemapLoader" }
        ]
      },
      "minItems": 1
    },
    "replacers": {
      "type": "array",
      "items": {
        "oneOf": [
          { "$ref": "#/definitions/StaticReplacer" }
        ]
      },
      "minItems": 1
    }
  },
  "additionalProperties": false,
  "required": ["testCase"],
  "definitions": {
    "FileLoader": {
      "type": "object",
      "properties": {
        "loader": {
          "enum": ["file"]
        },
        "options": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string"
            }
          },
          "required": ["filePath"],
          "additionalProperties": false
        }
      },
      "required": ["loader", "options"],
      "additionalProperties": false
    },
    "SitemapLoader": {
      "type": "object",
      "properties": {
        "loader": {
          "enum": ["sitemap"]
        },
        "options": {
          "type": "object",
          "properties": {
            "sitemap": {
              "type": "string"
            }
          },
          "required": ["sitemap"],
          "additionalProperties": false
        }
      },
      "required": ["loader", "options"],
      "additionalProperties": false
    },
    "StaticReplacer": {
      "type": "object",
      "properties": {
        "replacer": {
          "enum": ["static"]
        },
        "options": {
          "type": "object",
          "properties": {
            "replace": {
              "type": "string"
            },
            "with": {
              "type": "string"
            }
          },
          "required": ["replace", "with"],
          "additionalProperties": false
        }
      },
      "required": ["replacer", "options"],
      "additionalProperties": false
    }
  }
}